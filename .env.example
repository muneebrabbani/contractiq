OPENAI_API_KEY=
CHAT_MODEL=gpt-4o-mini
EMBEDDING_MODEL=text-embedding-3-small
RETRIEVAL_EMBEDDING_MODEL=text-embedding-3-large

# OpenShift `oc login` token (from the console's "Copy login command"), used
# by deploy scripts -- not consumed by the app itself.
OC_LOGIN_TOKEN=
